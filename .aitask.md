2026-04-24-T161604：为 Codex CLI 配置提示音

原始需求：
学习当前这个项目 这个项目是给claude这个工具安装提示技巧的 针对不同的事件，有不同的提示音

有，但分两类：

**1）任务完成提醒：有官方支持**
Codex CLI 支持 `notify` 外部通知钩子，`~/.codex/config.toml` 里可配置命令；Codex 会给它传 JSON payload。

**2）确权/审批提示：CLI 里有 TUI 通知，但外部 notify 支持不一定完整**
官方配置里有：

```toml
[tui]
notifications = ["agent-turn-complete", "approval-requested"]
notification_condition = "always"
notification_method = "auto"
```

`tui.notifications` 可限制事件类型，`approval-requested` 是社区 issue 里明确提到可用于桌面提示的事件；但外部 `notify` 钩子早期只稳定用于 turn complete，审批事件外部 hook 曾是独立诉求。

**推荐配置：**

```toml
notify = ["/Users/johnson/.codex/notify.sh"]

[tui]
notifications = ["agent-turn-complete", "approval-requested"]
notification_condition = "always"
notification_method = "auto"
```

macOS 上 `notify.sh` 可以这样：

```bash
#!/usr/bin/env bash
osascript -e 'display notification "Codex 需要你处理 / 或任务已完成" with title "Codex CLI" sound name "Glass"'
```

```bash
chmod +x ~/.codex/notify.sh
```

更省心方案：用 **Codex App**，官方说明它默认会在任务完成或需要 approval 时发通知，而且设置会和 CLI/IDE 共享一部分配置。

参考

请给codex cli 配置好提示音

解决方案：
- 新增项目脚本 `hooks/codex-notify.sh`，用于接收 Codex `notify` 的 JSON 负载，并按事件关键词区分提示音。
- 新增文档 `set-codex-notification.md`，说明 Codex CLI 在 macOS 下的安装与配置方式。
- 更新 `README.md`，补充 Codex CLI 的安装步骤、配置片段和默认声音说明。
- 已将脚本安装到 `/Users/johnson/.codex/notify.sh`，并在 `/Users/johnson/.codex/config.toml` 中写入：
  - `notify = ["/Users/johnson/.codex/notify.sh"]`
  - `[tui] notifications = ["agent-turn-complete", "approval-requested"]`
  - `notification_condition = "always"`
  - `notification_method = "auto"`
- 已验证 `~/.codex/config.toml` 的 TOML 语法正常，并使用测试负载验证 `~/.codex/notify.sh` 可执行。

优化建议：
- 如果后续确认 Codex 新版本对审批事件的外部 `notify` 负载更稳定，可把当前关键词兜底逻辑改成精确字段分流。
- 如果希望更强区分度，可继续把 `approval-requested`、`input needed`、`task complete` 分成三种不同声音。
- 若计划把本仓库同时作为 Claude 与 Codex 的统一技能包，下一步可补一个可交互的一键安装脚本，自动同步到 `~/.codex`。

2026-04-24-T163253：调整 Codex 默认完成提示音为 Blow

原始需求：
set Glass to Blow
for u as default one

解决方案：
- 已将 `/Users/johnson/.codex/notify.sh` 中 `DEFAULT_SOUND` 从 `Glass` 改为 `Blow`。
- 保持 `ACTION_SOUND="Funk"` 不变，因此审批/确认类提示音不受影响。
- 已使用完成事件测试负载执行脚本，确认脚本可正常运行。

优化建议：
- 如果后续你也想把审批提示音一起调整，我建议继续保留“完成音更轻、审批音更明显”的区分策略。

2026-04-24-T163610：为 Codex 增加 say 语音提示

原始需求：
你参考claude 的say 配置，给codex也配置上say的内容，可以吗

解决方案：
- 已参考 Claude 项目中的 `hooks/notification-sound.py` 文案风格，为 `/Users/johnson/.codex/notify.sh` 增加 `say` 语音提示。
- 当前 Codex 分流如下：
  - 审批类：`Funk` + `say "Permission Need"`
  - 确认类：`Funk` + `say "Confirmation Need"`
  - 完成类：`Blow` + `say "Task Finished"`
  - 其他通知：`Blow` + `say "Attention Need"`
- 同时保留 macOS 桌面通知弹窗，标题为 `Codex CLI`。
- 已使用 4 类测试负载完成回归验证，脚本执行正常。

优化建议：
- 如果后续你更偏向中文朗读，可把 `say` 文案改成中文，但 macOS 英文语音对英文短句的效果通常更稳定。
